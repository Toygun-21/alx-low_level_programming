#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * Code Description: Prints the name of the program
 * @argc: No of command line arguments
 * @argv: Array name
 * Return: 0 for success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
