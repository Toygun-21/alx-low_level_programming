#include "main.h"
/**
 * _isdigit - defines gits from  0 -9
 * @c - integer to check
 * Return: 0 if false  or 1 if true
 */
int _isdigit(int c)
{
if(c >= 48  && c <= 57)
return (1);
else
return (0);
}
